{% extends 'base.html.twig' %}

{% block title %}Articles
{% endblock %}

{% block body %}
	<h1>Articles</h1>

	<div class="card">
		<img src={{ asset('uploads/' ~ article.imageFilename )}} widht="100%" />
		<div class="card-body">
			<h5 class="card-title">{{ article.title }}</h5>
			<p class="card-text">{{ article.date ? article.date|date('Y-m-d') : '' }}</p>
			<p class="card-title">{{ article.content }}</p>
			<a href="{{ path('app_category_index') }}" class="btn btn-primary">Retour Au catégorie</a>
		</div>
	</div>

{% endblock %}
